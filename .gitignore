# === Python (engine/) ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/
env/

# === Claude / local tooling ===
**/.claude/settings.local.json

# === Obsidian (vault) ===
# Keep .obsidian config tracked (plugins, appearance) so settings sync; ignore
# only the per-session workspace state that churns constantly.
Vault/.obsidian/workspace.json
Vault/.obsidian/workspace-mobile.json
.trash/

# Engine-derived browsable index — regenerate with `vault-contrib index`.
Vault/Agent/INDEX.md

# === Syncthing ===
.stfolder/

# === OS / editor ===
.DS_Store
Thumbs.db
